# The Lint Hook

Never edit `lint_check.py` to make it always pass — the linter is the check,
not an obstacle. When a hook reports a lint failure, fix the actual line
length in the code file, not the linter's threshold.
